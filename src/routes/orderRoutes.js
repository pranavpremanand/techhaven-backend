// const express = require("express");
// const protect = require("../middlewares/authMiddleware");

// const router = express.Router();

// //  user address
// router.post("/checkout", protect, checkoutProduct);
// router.get("/", (req, res) => {
//   res.send("Auth Route");
// });

// module.exports = router;
